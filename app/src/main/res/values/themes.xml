<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Light theme specific variables -->
    <style name="Base.Theme.Foundation.Light" parent="Theme.Material3.DynamicColors.Light">
        <item name="android:navigationBarColor">#60000000</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <style name="Base.Theme.Foundation" parent="Base.Theme.Foundation.Light" />

    <!-- Common theme variables -->
    <style name="Theme.Foundation" parent="Base.Theme.Foundation">
        <item name="android:includeFontPadding">false</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="actionBarSize">@dimen/internal_action_bar_size</item>
        <item name="dialogTheme">@style/ThemeOverlay.Foundation.Dialog</item>
        <item name="popupMenuStyle">@style/Widget.Foundation.PopupMenu</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <!-- Splash themes -->
    <style name="SplashTheme" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_magisk_padded</item>
    </style>

    <style name="StubSplashTheme" parent="SplashTheme"/>

</resources>
